import { Station } from '../types';

export const getStations = (): Station[] => {
  // This is no longer used. Stations are now loaded dynamically from external scripts in the KiwiSdrView component.
  return [];
};